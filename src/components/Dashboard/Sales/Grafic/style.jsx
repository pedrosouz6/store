import styled from 'styled-components';

import { Colors } from '../../../../styles/Default';

const {
    background_primary
} = Colors.light;   

export const Container = styled.article `
    width: 100%;
    flex: 1 1 320px;

    .dashboard--sales__grafic {
        width: 100%;
    }

`